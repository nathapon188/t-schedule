[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "22"

# Single page app: everything that is not a file or the API falls back to index.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
